# 🚀 خطة ومعمارية بناء المنظومة السحابية (SaaS Architecture & System Blueprint)

---

## 📌 1. الملخص التنفيذي وفكرة المنصة (Executive Summary)

### الفكرة الأساسية:
تحويل نظام إدارة السناتر والمعلمين من نظام محلي مغلق (Single-Tenant) إلى منصة سحابية مركزية متعددة المشتركين (**Multi-Tenant SaaS**):
* يستطيع أي معلم أو سنتر الدخول إلى المنصة، التسجيل بنفسه، وتخصيص مساحة عمل مستقلة له فوراً مع فترة تجربة مجانية (Free Trial).
* كل معلم يحصل على:
  1. **موقع تعريفي وبوابة حجز طلاب:** على المسار `/t/{tenant_slug}` (مثال: `/t/mr-diaa`).
  2. **بوابة طالب:** `/t/{tenant_slug}/student/login` (مجموعات، امتحانات أونلاين، واجبات).
  3. **بوابة ولي أمر:** `/t/{tenant_slug}/parent/login` (حضور، درجات، سداد إلكتروني، كشف حساب).
  4. **لوحة تحكم كاملة (Filament Admin):** على `/admin/{tenant_slug}`.
* مالك المنصة لديه لوحة تحكم عليا مستقلة (**SuperAdmin Panel**) على الرابط `/super-admin` للتحكم في المشتركين، الباقات، ومتابعة الأرباح والاشتراكات.

---

## 🏗️ 2. استراتيجية الـ Multi-Tenancy (لماذا وكيف اخترنا هذا النمط؟)

### النمط المعتمد: **Single Database with Row-Level Isolation (قاعدة بيانات واحدة مع عزل الصفوف)**

```
┌────────────────────────────────────────────────────────────────────────┐
│                        قاعدة بيانات المنظومة المركزية (MySQL)            │
├────────────────────────────────────────────────────────────────────────┤
│ 🏢 جدول السناتر (tenants):                                            │
│    - ID: 1 | Name: سنتر النخبة  | Slug: el-nokhba  | Plan: Pro         │
│    - ID: 2 | Name: سنتر الأستاذ ضياء | Slug: mr-diaa    | Plan: Standard │
├────────────────────────────────────────────────────────────────────────┤
│ 👥 جدول الطلاب (students):                                             │
│    - ID: 101 | Name: أحمد محمد  | tenant_id: 2 (خاص بضياء فقط)        │
│    - ID: 102 | Name: محمود علي  | tenant_id: 1 (خاص بالنخبة فقط)      │
├────────────────────────────────────────────────────────────────────────┤
│ 📝 جداول الحضور والامتحانات والواجبات والماليات (مربوطة بـ tenant_id)  │
└────────────────────────────────────────────────────────────────────────┘
```

### لماذا هذا الاختيار؟
1. **سهولة وسرعة الترقية والصيانة:** تحديث أو تشغيل `php artisan migrate` واحد يحدث قاعدة البيانات لكل المشتركين فوراً.
2. **توفير هائل في الموارد والاستضافة:** لا نحتاج لإنشاء مئات قواعد البيانات وحسابات MySQL.
3. **أداء وسرعة عالية:** استعلامات مفهرسة (Indexed) على `tenant_id`.
4. **أمان تام وعزل مطلق للبيانات:** بفضل تقنية الـ **Global Scope** البرمجية.

---

## ⚙️ 3. كيف تم بناء المنظومة تقنياً؟ (الهندسة البرمجية خطوة بخطوة)

---

### الخطوة 1: طبقة قاعدة البيانات (Database & Migrations)
1. إنشاء جدول المشتركين `tenants`:
   - الحقول: `id`, `name`, `slug` (فريد), `email`, `phone`, `logo`, `favicon`, `is_active`, `trial_ends_at`, `settings` (JSON).
2. إضافة عمود `tenant_id` مفهرس مع علاقة Cascade إلى كل الجداول الأكاديمية والمالية:
   - `students`, `groups`, `group_sessions`, `attendances`, `exams`, `questions`, `homeworks`, `student_payments`, `expenses`, `study_materials`, `tenant_settings` ... إلخ.
3. إنشاء جداول إدارة الـ SaaS:
   - `plans`: باقات الاشتراك (الاسم، السعر، مدة الاشتراك، المميزات).
   - `subscriptions`: اشتراكات السناتر وتواريخ البداية والنهاية والحالة (`active`, `expired`, `cancelled`).
   - `subscription_payments`: سجل فواتير ومدفوعات المدرسين للمنصة.

---

### الخطوة 2: عزل البيانات التلقائي (Automatic Scoping via Trait)
أنشأنا الـ Trait السحري: [`app/Traits/BelongsToTenant.php`](file:///d:/project/mohammed_elgandy/app/Traits/BelongsToTenant.php):
* **عند القراءة أو التعديل أو الحذف:** يضيف `Global Scope` تلقائياً يضمن ألا يرى المعلم أو الطالب إلا البيانات التي تطابق:
  ```sql
  WHERE tenant_id = current_tenant_id
  ```
* **عند إنشاء أي سجل جديد:** يستمع لحدث `creating` ويحقن `tenant_id` تلقائياً من المدرس الحالي دون الحاجة لكتابتها يدوياً.

---

### الخطوة 3: دورة حياة الطلب وحقن السياق (Request Lifecycle & Context Resolution)
* أنشأنا خدمة Singleton: [`app/Services/TenantContext.php`](file:///d:/project/mohammed_elgandy/app/Services/TenantContext.php) التي تحتفظ ببيانات المدرس الحالي في الذاكرة أثناء تنفيذ الـ Request.
* أنشأنا الـ Middleware المسؤول: [`app/Http/Middleware/ResolveTenant.php`](file:///d:/project/mohammed_elgandy/app/Http/Middleware/ResolveTenant.php):
  1. يقرأ الـ `{tenant}` من الرابط.
  2. يبحث عن المعلم في قاعدة البيانات عبر الـ `slug`.
  3. يثبته في `TenantContext`.
  4. يمرره كمتغير `$currentTenant` إلى جميع واجهات الـ Blade المشتركة.

---

### الخطوة 4: عزل لوحات التحكم (Dual Filament Panels)
قسمنا لوحات Filament إلى لوحتين مستقلتين تماماً:

1. **لوحة المعلم (`AdminPanelProvider.php`):**
   - الرابط: `/admin/{tenant}`
   - تستخدم ميزة Filament المدمجة: `.tenant(Tenant::class, slugAttribute: 'slug')`
   - تدمج تلقائياً مع `SyncFilamentTenant` وسيط المزامنة.
   - تمنع أي مدرس من الوصول لبيانات مدرس آخر.

2. **لوحة السوبر أدمن (`SuperAdminPanelProvider.php`):**
   - الرابط: `/super-admin`
   - غير مقيدة بمدرس معين؛ مخصصة لمالك المنصة للتحكم في باقات الاشتراك، قبول المشتركين الجدد، وإحصائيات المنظومة.

---

### الخطوة 5: هندسة البوابات العامة والمواقع (Portals & Landing Architecture)

تم تجميع المسارات في [`routes/web.php`](file:///d:/project/mohammed_elgandy/routes/web.php) بنظام الطبقات:

| البوابة | المسار (Route Pattern) | الكنترولر المسؤول | الغرض |
|---|---|---|---|
| **تسويق المنصة** | `/` و `/pricing` و `/register` | `PlatformController` | عرض مزايا المنصة، أسعار الباقات، والتسجيل الفوري |
| **موقع المعلم** | `/t/{tenant}/` | `HomeController` | بروفايل المعلم، المواعيد، وحجز الطلاب الجدد |
| **بوابة الطالب** | `/t/{tenant}/student/*` | `StudentPortalController` | دخول الطالب، حل الواجبات، وأداء الامتحانات |
| **امتحانات الطالب** | `/t/{tenant}/student/exams/*`| `OnlineExamController` | شاشة الامتحان التفاعلية، المؤقت، والنتيجة الفورية |
| **بوابة ولي الأمر** | `/t/{tenant}/parent/*` | `ParentPortalController` | تقارير الحضور والغياب، الدرجات، وسداد المصروفات |
| **تقارير ولي الأمر**| `/t/{tenant}/parent/exams/*` | `OnlineExamController` | استعراض نتيجة ابنه والشرح النموذجي |
| **سداد اشتراك المعلم**| `/t/{tenant}/subscription/*` | `SubscriptionPaymentController` | تجديد المعلم لاشتراكه بالمنصة |

---

### الخطوة 6: حماية الاشتراكات (Subscription Enforcement Middleware)
أنشأنا الوسيط [`app/Http/Middleware/CheckSubscription.php`](file:///d:/project/mohammed_elgandy/app/Http/Middleware/CheckSubscription.php):
* يفحص ما إذا كان السنتر في فترة تجربة مجانية سارية (`trial_ends_at`) أو لديه اشتراك فعال.
* إذا انتهت الفترة، يقوم بإغلاق اللوحة تلقائياً وتوجيهه لصفحة تجديد الاشتراك وسداد الرسوم للمنصة.

---

## 🧩 4. ملخص الموديولات الأكاديمية المبنية داخل النظام

1. **إدارة الطلاب واستيراد Excel الذكي:** إنشاء أكواد وكروت QR للطلاب مع استيراد الدفعات الكبيرة في الخلفية عبر طوابير (Queues).
2. **المجموعات والجداول:** تسكين الطلاب وضبط مواعيد الحصص وحساب أسعار الاشتراكات.
3. **الحضور والغياب والباركود:** مسح كارت الطالب عبر كاميرا الهاتف أو قارئ الباركود، مع إرسال رسائل WhatsApp تلقائية لولي الأمر.
4. **بنك الأسئلة والذكاء الاصطناعي:** استيراد الأسئلة من ملفات Word/Excel وتصنيفها حسب الصعوبة والموضوع.
5. **الامتحانات والكويزات أونلاين:** محرك امتحانات متكامل بمؤقت تنازلي، تصحيح تلقائي، وتحديد نقاط ضعف الطالب.
6. **الواجبات المنزلية:** إسناد الواجبات، استقبال الحلول النصية أو الورقية (PDF/صور)، والتصحيح ورصد الملاحظات.
7. **الماليات وسداد الرسوم أونلاين:** حساب رصيد الطالب التراكمي (Ledger)، ودعم تحويلات فودافون كاش وانستاباي مع رفع الإيصالات واعتمادها.
8. **الملازم والمخازن:** تتبع تسليم الكتب والملازم ومتابعة سدادها.

---

## 🎯 5. النقاط الذهبية لشرح المشروع للتيم (Talking Points for the Team)

عند الجلوس مع فريق التطوير، يمكنك توضيح الآتي:
1. **أين نكتب منطق الأعمال؟** في مجلد `app/Services/` حصراً، والكنترولر فقط يستدعي الـ Service (Clean Architecture).
2. **كيف نضمن عزل أي جدول جديد في المستقبل؟** 
   - إضافة عمود `$table->foreignId('tenant_id')->constrained()->cascadeOnDelete();` في الـ Migration.
   - استخدام Trait `use BelongsToTenant;` داخل الـ Model.
   - تعريف العلاقة داخل موديل `Tenant.php`.
3. **كيف نستدعي الروابط في الواجهات؟**
   - دائماً نمرر الـ `tenant` في الـ Route: `route('tenant.student.dashboard', ['tenant' => $currentTenant->slug])`.
4. **أين تجد وثائق الكود التفصيلية؟**
   - تم توثيق جميع الـ 19 سيرفس والـ 11 موديول بالتفصيل داخل ملف [`DEVELOPER_GUIDE.md`](file:///d:/project/mohammed_elgandy/DEVELOPER_GUIDE.md).
